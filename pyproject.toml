[project]
name = "chinamoney-fx-daily"
version = "0.1.0"
description = "Daily, auditable China Money FX market-data collector"
requires-python = ">=3.11"
dependencies = ["playwright>=1.55,<2"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
